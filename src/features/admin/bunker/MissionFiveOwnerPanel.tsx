import type { MissionFiveScreenModel } from '../../bunker/v2/MissionFiveScreen';
export function MissionFiveOwnerPanel({model}:{model:MissionFiveScreenModel}){return<section className="admin-bunker-mission" aria-label="Задание 5 · контроль ведущего"><h3>{model.title}</h3><p>90 секунд. Голоса анонимны для ведущего — видны только суммы по вагонам.</p><ul>{model.wagons.map(w=><li key={w.wagonId}><strong>{w.label}</strong><span>{w.routeChoice?`МАРШРУТ ${w.routeChoice}`:`A ${w.votesA} · B ${w.votesB}`}</span><small>{w.status==='completed'?'ЗАВЕРШЕНО':`нужно ${w.required}`}</small></li>)}</ul></section>}
