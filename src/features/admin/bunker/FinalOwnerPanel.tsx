import { useState } from 'react';import type { FinalScreenModel } from '../../bunker/v2/FinalScreen';export function FinalOwnerPanel({model,onAddTime,onHint,onEmergencyOpen}:{model:FinalScreenModel;onAddTime?:()=>Promise<void>|void;onHint?:()=>Promise<void>|void;onEmergencyOpen?:()=>Promise<void>|void}){const[confirmOpen,setConfirmOpen]=useState(false),[busy,setBusy]=useState(false);const run=(action?:()=>Promise<void>|void)=>{setBusy(true);Promise.resolve(action?.()).finally(()=>setBusy(false));};return <section className="admin-bunker-mission admin-bunker-final" aria-label="Финал · контроль ведущего"><header><h3>ФИНАЛ · 30 МИНУТ ДО БУНКЕРА</h3><strong>{Math.floor(model.remainingSeconds/60)}:{String(model.remainingSeconds%60).padStart(2,'0')}</strong></header><p>{model.solved}/{model.total} параметров подтверждено · неудачных проверок: {model.wrongAttempts}</p><p>Ответы намеренно скрыты. Пульт помогает только временем и подсказками.</p><div className="admin-bunker-final__actions"><button type="button" disabled={busy||model.unlocked} onClick={()=>run(onAddTime)}>+2 МИНУТЫ</button><button type="button" disabled={busy||model.unlocked||model.hintLevel>=3} onClick={()=>run(onHint)}>ДАТЬ ПОДСКАЗКУ</button>{!confirmOpen?<button type="button" disabled={busy||model.unlocked} onClick={()=>setConfirmOpen(true)}>АВАРИЙНО ОТКРЫТЬ</button>:<button type="button" disabled={busy||model.unlocked} onClick={()=>{run(onEmergencyOpen);setConfirmOpen(false);}}>ПОДТВЕРДИТЬ АВАРИЙНОЕ ОТКРЫТИЕ</button>}</div>{confirmOpen&&<p role="alert">Аварийное открытие завершит головоломку без правильного кода. Используйте только если гостям нужна помощь или заканчивается время.</p>}</section>}
