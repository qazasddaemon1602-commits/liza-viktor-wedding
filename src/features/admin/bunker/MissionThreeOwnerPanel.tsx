import type{MissionThreeScreenModel}from'../../bunker/v2/MissionThreeScreen';export function MissionThreeOwnerPanel({model}:{model:MissionThreeScreenModel}){return<section className="admin-bunker-mission" aria-label="Задание 3 · контроль ведущего"><h3>{model.title}</h3><p>Ведущий видит только прогресс. Решение остаётся за капитаном каждого вагона.</p><ul>{model.wagons.map(w=><li key={w.wagonId}><strong>{w.label}</strong><span>{w.status==='completed'?`ЗАВЕРШЕНО · ${w.solvedCount} ИЗ 5`:'В РАБОТЕ'}</span></li>)}</ul></section>}
