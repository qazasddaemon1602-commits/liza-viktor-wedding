import type { MissionSixScreenModel } from '../../bunker/v2/MissionSixScreen';export function MissionSixOwnerPanel({model}:{model:MissionSixScreenModel}){const ready=model.wagons.filter(w=>w.consensusReady).length;return <section className="admin-bunker-mission" aria-label="Задание 6 · контроль ведущего"><h3>{model.title}</h3><p>Фрагменты: {model.fragmentsRevealed}/{model.fragmentsTotal}. Согласовали: {ready}/{model.wagons.length}. Конкретные варианты и финальный код на пульте не показываются.</p><ul>{model.wagons.map(w=><li key={w.wagonId}><strong>{w.label}</strong><span>{w.consensusReady?'СОГЛАСОВАНО':'СВЕРЯЮТ'}</span></li>)}</ul></section>}
