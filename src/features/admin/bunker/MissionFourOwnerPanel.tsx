import type{MissionFourScreenModel}from'../../bunker/v2/MissionFourScreen';export function MissionFourOwnerPanel({model}:{model:MissionFourScreenModel}){return<section className="admin-bunker-mission" aria-label="Задание 4 · контроль ведущего"><h3>{model.title}</h3><p>Текст сообщений скрыт от пульта: здесь только состояние групп и обменов.</p><ul>{model.groups.map(g=><li key={g.groupKey}><strong>{g.labels.join(' + ')}</strong><span>{g.phase==='resolved'?'ГОТОВО':g.phase==='answer'?'ОБЩИЙ ОТВЕТ':'СВЯЗЬ И ОБМЕН'}</span><small>{g.answeredWagons}/{g.totalWagons} ответили · обменов {g.tradeCount}</small></li>)}</ul></section>}
