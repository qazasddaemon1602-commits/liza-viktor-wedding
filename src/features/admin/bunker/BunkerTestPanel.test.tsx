import { render,screen } from '@testing-library/react';import{describe,expect,it,vi}from'vitest';import{BunkerTestPanel}from'./BunkerTestPanel';describe('BunkerTestPanel',()=>{it('makes rehearsal actions understandable without technical language',()=>{render(<BunkerTestPanel state={{gameMode:'test',globalState:'MISSION_03'}} onSeed={vi.fn()} onPrepare={vi.fn()} onAccelerate={vi.fn()} onSimulate={vi.fn()} onResetProgress={vi.fn()} onResetRegistrations={vi.fn()} onFullReset={vi.fn()} onSetInventory={vi.fn()} onSetWagonState={vi.fn()}/>);expect(screen.getByRole('heading',{name:'РЕПЕТИЦИЯ ИГРЫ'})).toBeInTheDocument();expect(screen.getByRole('button',{name:'20 ГОСТЕЙ'})).toBeInTheDocument();expect(screen.getByRole('button',{name:'УСКОРИТЬ НА 1 МИНУТУ'})).toBeInTheDocument();expect(screen.getByRole('button',{name:'СИМУЛИРОВАТЬ ТЕКУЩИЙ ЭТАП'})).toBeInTheDocument();expect(screen.getByText(/работают только в режиме репетиции/i)).toBeInTheDocument();});});
