import { renderHook, waitFor } from '@testing-library/react';import { describe, expect, it, vi } from 'vitest';import type { BunkerV2ActiveGuestRuntime } from './v2/contracts';import { useGuestBunkerLiveState, type GuestBunkerLiveDependencies } from './useGuestBunkerLiveState';
const runtime={contractVersion:2,status:'active',serverNow:'2026-08-30T18:50:00Z',state:'MISSION_06',planVersion:1,runNonce:'run',viewer:{kind:'guest',guest:{id:'g',realName:'Анна'},wagon:{number:1,label:'ВАГОН №1'}},character:{profileKey:'architect',profileVersion:1,profession:'Архитектор',health:'здоров',visibleSkill:'планы',specialAbility:'plan_analysis',abilityDescription:'анализ',abilityUsesRemaining:1,status:'saved',m01Eligibility:'frozen_member',hiddenTraitRevealed:false},currentMission:{instanceId:'m6',instanceVersion:1,code:'MISSION_06',status:'active',scope:'global'}} satisfies BunkerV2ActiveGuestRuntime;
const oldM02={contractVersion:2 as const,status:'completed' as const,serverNow:'2026-08-30T18:50:00Z',deadlineAt:'2026-08-30T18:40:00Z',instanceId:'m2',instanceVersion:1,title:'Чёрный ящик',subtitle:'x',intro:'x',wagon:{number:1,label:'ВАГОН №1'},evidence:Array.from({length:6},(_,i)=>({key:`e${i}`,label:`e${i}`,body:'x'})),questions:[{key:'a',prompt:'a',options:['1','2']},{key:'b',prompt:'b',options:['1','2']},{key:'c',prompt:'c',options:['1','2']}],attemptCount:1,attemptsRemaining:0,selectedAnswers:['1','1','1'],ability:null,outcome:'success' as const,archiveUnlocked:'BK-17' as const};
const currentM06={contractVersion:2 as const,status:'active' as const,serverNow:'2026-08-30T18:50:00Z',deadlineAt:'2026-08-30T18:58:00Z',instanceId:'m6',instanceVersion:1,title:'Общий протокол',intro:'x',viewer:{wagonId:'w1',wagonNumber:1,canVote:true},privateFragment:{key:'f1',label:'f',body:'x'},fragmentShared:false,revealedFragments:[],fragmentsRevealed:0,fragmentsTotal:2,options:[{key:'A' as const,title:'A',summary:'A'},{key:'B' as const,title:'B',summary:'B'},{key:'C' as const,title:'C',summary:'C'}],selectedVote:null,wagonConsensus:[{wagonId:'w1',label:'ВАГОН №1',votesA:0,votesB:0,votesC:0,required:2,consensus:null}],ability:null};
function deps():GuestBunkerLiveDependencies{return{getDeviceKey:()=> 'device',load:vi.fn().mockResolvedValue({status:'idle',serverNow:runtime.serverNow}),loadRuntime:vi.fn().mockResolvedValue(runtime),loadMissionTwo:vi.fn().mockResolvedValue(oldM02),loadMissionSix:vi.fn().mockResolvedValue(currentM06),submitMission:vi.fn(),submitFinalCode:vi.fn(),subscribeToRefresh:()=>vi.fn()};}
describe('current-stage projection guard',()=>{it('removes completed old mission projections when V2 runtime has advanced',async()=>{const{result}=renderHook(()=>useGuestBunkerLiveState({dependencies:deps()}));await waitFor(()=>expect(result.current.runtime).toMatchObject({state:'MISSION_06'}));await waitFor(()=>expect(result.current.missionSix?.status).toBe('active'));expect(result.current.missionTwo).toBeUndefined();});});
