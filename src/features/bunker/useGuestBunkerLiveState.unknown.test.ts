import { renderHook, waitFor } from '@testing-library/react';import { describe, expect, it, vi } from 'vitest';import { useGuestBunkerLiveState, type GuestBunkerLiveDependencies } from './useGuestBunkerLiveState';
const story={contractVersion:2 as const,status:'active' as const,serverNow:'2026-08-30T19:00:00Z',deadlineAt:'2026-08-30T19:01:00Z',title:'Неизвестный пассажир',dossierId:'BK-17',lead:'Неизвестное досье',sector:'04',accessCode:'4719',bunkerRevealed:true,recoveredBy:'archive_recovery' as const,storyPoints:['Пункт']};
const runtime={contractVersion:2 as const,status:'active' as const,serverNow:story.serverNow,state:'UNKNOWN_PASSENGER' as const,planVersion:1,runNonce:'run',viewer:{kind:'guest' as const,guest:{id:'g',realName:'Анна'},wagon:{number:1,label:'ВАГОН №1'}},character:{profileKey:'architect',profileVersion:1,profession:'Архитектор',health:'здоров',visibleSkill:'планы',specialAbility:'plan_analysis',abilityDescription:'анализ',abilityUsesRemaining:1,status:'saved' as const,m01Eligibility:'frozen_member' as const,hiddenTraitRevealed:false as const},currentMission:{instanceId:'story',instanceVersion:1,code:'UNKNOWN_PASSENGER' as const,status:'active' as const,scope:'global' as const}};
const deps:GuestBunkerLiveDependencies={getDeviceKey:()=> 'device',load:vi.fn().mockResolvedValue({status:'idle',serverNow:story.serverNow}),loadRuntime:vi.fn().mockResolvedValue(runtime),loadUnknownPassenger:vi.fn().mockResolvedValue(story),submitMission:vi.fn(),submitFinalCode:vi.fn(),subscribeToRefresh:()=>vi.fn()};
describe('unknown passenger live state',()=>{it('projects the 60-second dossier while UNKNOWN_PASSENGER is current',async()=>{const{result}=renderHook(()=>useGuestBunkerLiveState({dependencies:deps}));await waitFor(()=>expect(result.current.unknownPassenger).toMatchObject({dossierId:'BK-17',remainingSeconds:60,accessCode:'4719'}));});});
