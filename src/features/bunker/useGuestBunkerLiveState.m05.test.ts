import { act, renderHook, waitFor } from '@testing-library/react';import { describe, expect, it, vi } from 'vitest';import { useGuestBunkerLiveState, type GuestBunkerLiveDependencies } from './useGuestBunkerLiveState';
const m05={contractVersion:2 as const,status:'active' as const,serverNow:'2026-08-30T18:40:00.000Z',deadlineAt:'2026-08-30T18:41:30.000Z',instanceId:'m5',instanceVersion:1,title:'Один шанс',intro:'90 секунд',wagon:{number:1,label:'ВАГОН №1'},routes:[{key:'A' as const,title:'Технический тоннель',description:'Быстро',risk:'Риск'},{key:'B' as const,title:'Обходной путь',description:'Безопасно',risk:'Дольше'}],selectedVote:null,voteCounts:{A:0,B:0,total:0,required:4},ability:null};
function deps(overrides:Partial<GuestBunkerLiveDependencies>):GuestBunkerLiveDependencies{return{getDeviceKey:()=> 'device',load:vi.fn().mockResolvedValue({status:'idle',serverNow:m05.serverNow}),loadRuntime:vi.fn().mockResolvedValue({status:'idle',serverNow:m05.serverNow}),submitMission:vi.fn(),submitFinalCode:vi.fn(),subscribeToRefresh:()=>vi.fn(),...overrides}as GuestBunkerLiveDependencies;}
describe('useGuestBunkerLiveState M05',()=>{it('loads route projection and casts a route vote',async()=>{const loadMissionFive=vi.fn().mockResolvedValue(m05);const castMissionFiveVote=vi.fn().mockResolvedValue({contractVersion:2,status:'accepted'});const dependencies=deps({loadMissionFive,castMissionFiveVote});const{result}=renderHook(()=>useGuestBunkerLiveState({dependencies}));await waitFor(()=>expect(result.current.missionFive).toMatchObject({title:'Один шанс',remainingSeconds:90}));await act(async()=>result.current.castMissionFiveVote('A'));expect(castMissionFiveVote).toHaveBeenCalledWith('device',expect.objectContaining({instanceId:'m5',vote:'A',commandId:expect.any(String)}));});});
