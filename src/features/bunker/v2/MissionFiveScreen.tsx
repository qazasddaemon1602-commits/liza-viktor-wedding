export type MissionFiveScreenModel={title:string;remainingSeconds:number;wagons:Array<{wagonId:string;label:string;status:'active'|'completed';votesA:number;votesB:number;required:number;routeChoice:'A'|'B'|null}>};
export function MissionFiveScreen({model}:{model:MissionFiveScreenModel}){return<section className="bunker-v2-screen bunker-v2-screen--m05" aria-label="Задание 5 · общий экран"><header><div><p>ЗАДАНИЕ 5 · ОДИН ШАНС</p><h1>{model.title.toLocaleUpperCase('ru-RU')}</h1></div><time><strong>{Math.max(0,Math.floor(model.remainingSeconds))}</strong><small>СЕКУНД</small></time></header><main><p>КАЖДЫЙ ВАГОН ВЫБИРАЕТ МАРШРУТ A ИЛИ B</p><div className="bunker-wagon-grid" data-count={model.wagons.length}>{model.wagons.map(w=><article key={w.wagonId} className={w.status==='completed'?'is-complete':''}><h2>{w.label}</h2><strong>{w.routeChoice?`МАРШРУТ ${w.routeChoice}`:`A ${w.votesA} · B ${w.votesB}`}</strong><span>{w.status==='completed'?'РЕШЕНИЕ ПРИНЯТО':`НУЖНО ${w.required} ГОЛОСОВ`}</span></article>)}</div></main></section>}
