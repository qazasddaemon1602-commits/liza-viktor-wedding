import { render, screen } from '@testing-library/react';import { describe, expect, it } from 'vitest';import { UnknownPassengerPlayer } from './UnknownPassengerPlayer';
describe('UnknownPassengerPlayer',()=>{it('shows the recovered dossier, sector and code with no gameplay form',()=>{render(<UnknownPassengerPlayer model={{remainingSeconds:60,title:'Неизвестный пассажир',dossierId:'BK-17',lead:'В архиве найдено неизвестное досье.',sector:'04',accessCode:'4719',recoveredBy:'archive_recovery',storyPoints:['Пассажира нет в списке.','Бункер существует.']}}/>);expect(screen.getByRole('region',{name:'Сюжет · Неизвестный пассажир'})).toHaveTextContent('BK-17');expect(screen.getByText(/SECTOR 04/i)).toBeInTheDocument();expect(screen.getByText(/4719/)).toBeInTheDocument();expect(screen.queryByRole('textbox')).not.toBeInTheDocument();});});
