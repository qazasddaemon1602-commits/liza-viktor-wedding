export type FinalScreenModel={remainingSeconds:number;solved:number;total:number;wrongAttempts:number;unlocked:boolean;hintLevel:number;timeAdjustmentSeconds?:number};function timer(s:number){const n=Math.max(0,Math.floor(s));return`${String(Math.floor(n/60)).padStart(2,'0')}:${String(n%60).padStart(2,'0')}`;}export function FinalScreen({model}:{model:FinalScreenModel}){return <section className={`bunker-v2-screen bunker-v2-final-screen${model.unlocked?' is-unlocked':''}`} aria-label="Финал · общий экран"><header><div><p>ЭКСТРЕННОЕ СООБЩЕНИЕ</p><h1>{model.unlocked?'ДОСТУП ПОЛУЧЕН':'ЕДИНСТВЕННАЯ БЕЗОПАСНАЯ ТОЧКА — БУНКЕР'}</h1></div><time>{timer(model.remainingSeconds)}</time></header><main>{model.unlocked?<><strong>ШЛЮЗ РАЗБЛОКИРОВАН</strong><p>ПРИГОТОВЬТЕСЬ К ОТКРЫТИЮ БУНКЕРА</p></>:<><p>СОБЕРИТЕ ДАННЫЕ ВСЕХ ВАГОНОВ И ВВЕДИТЕ ИХ В ТЕРМИНАЛ НА ТЕЛЕФОНАХ</p><strong>{model.solved} / {model.total} ПАРАМЕТРОВ</strong>{model.wrongAttempts>0&&<span>ПРОВЕРОК: {model.wrongAttempts}</span>}{model.hintLevel>0&&<small>СИСТЕМА ВЫДАЛА ПОДСКАЗКУ УРОВНЯ {model.hintLevel}</small>}</>}</main><footer>ПОЕЗД ИДЁТ К ТОЧКЕ ПРИБЫТИЯ · ТАЙМЕР СЕРВЕРНЫЙ</footer></section>}
