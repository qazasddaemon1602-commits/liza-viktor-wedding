import { render, screen } from '@testing-library/react';import { describe, expect, it } from 'vitest';import { UnknownPassengerScreen } from './UnknownPassengerScreen';
describe('UnknownPassengerScreen',()=>{it('reveals the Bunker story without broadcasting the access code',()=>{render(<UnknownPassengerScreen model={{remainingSeconds:60,title:'Неизвестный пассажир',dossierId:'BK-17',sector:'04'}}/>);expect(screen.getByRole('region',{name:'Неизвестный пассажир · общий экран'})).toHaveTextContent('BK-17');expect(screen.getByText(/SECTOR 04/i)).toBeInTheDocument();expect(screen.queryByText('4719')).not.toBeInTheDocument();});});
