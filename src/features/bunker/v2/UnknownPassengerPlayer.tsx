export type UnknownPassengerPlayerModel={remainingSeconds:number;title:string;dossierId:string;lead:string;sector:string;accessCode:string;recoveredBy:'common_protocol'|'archive_recovery';storyPoints:string[]};function timer(s:number){const n=Math.max(0,Math.floor(s));return`${String(Math.floor(n/60)).padStart(2,'0')}:${String(n%60).padStart(2,'0')}`;}export function UnknownPassengerPlayer({model}:{model:UnknownPassengerPlayerModel}){return <section className="bunker-v2-mission bunker-v2-unknown-passenger" aria-label="Сюжет · Неизвестный пассажир"><header className="bunker-v2-mission__header"><div><span>АРХИВНАЯ АНОМАЛИЯ</span><h1>{model.title}</h1></div><time>{timer(model.remainingSeconds)}</time></header><div className="bunker-v2-dossier"><span>ДОСЬЕ</span><strong>{model.dossierId}</strong><p>{model.lead}</p></div><ol>{model.storyPoints.map((point,index)=><li key={`${index}:${point}`}>{point}</li>)}</ol><div className="bunker-v2-story-reveal"><span>БЕЗОПАСНАЯ ТОЧКА</span><h2>БУНКЕР · SECTOR {model.sector}</h2><strong>КОД ДОСТУПА · {model.accessCode}</strong><small>{model.recoveredBy==='common_protocol'?'Данные подтверждены общим протоколом.':'Недостающие данные восстановлены из резервного архива BK-17.'}</small></div></section>}
