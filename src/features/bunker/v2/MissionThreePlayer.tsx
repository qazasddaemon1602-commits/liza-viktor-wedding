import{useEffect,useState}from'react';import{bunkerItemLabel}from'./labels';import type{MissionThreeAbility,MissionThreeCommitment,MissionThreeInventoryItem,MissionThreeProblem}from'./m03.service';
export type MissionThreePlayerReadModel={instanceId:string;instanceVersion:number;status:'active'|'completed';remainingSeconds:number;title:string;intro:string;memberRole:'captain'|'member';problems:MissionThreeProblem[];inventory:MissionThreeInventoryItem[];selectedProblems:string[];ability:MissionThreeAbility;pendingCommitments:MissionThreeCommitment[];connection:'online'|'reconnecting';outcome?:Record<string,unknown>};function timer(s:number){const n=Math.max(0,Math.floor(s));return`${String(Math.floor(n/60)).padStart(2,'0')}:${String(n%60).padStart(2,'0')}`;}
export function MissionThreePlayer({model,onConfirm,onUseAbility}:{model:MissionThreePlayerReadModel;onConfirm?:(keys:string[])=>Promise<void>|void;onUseAbility?:(problemKey:string)=>Promise<void>|void}){const[selected,setSelected]=useState<string[]>(model.selectedProblems);const[busy,setBusy]=useState(false);useEffect(()=>setSelected(model.selectedProblems),[model.instanceId,model.selectedProblems]);const captain=model.memberRole==='captain';const toggle=(key:string)=>setSelected((current)=>current.includes(key)?current.filter(x=>x!==key):current.length<3?[...current,key]:current);return<section className="bunker-v2-mission bunker-v2-mission--m03" aria-label="Задание 3 · Аварийный запас"><header className="bunker-v2-mission__header"><div><span>ЗАДАНИЕ 3</span><h1>{model.title}</h1></div><time>{timer(model.remainingSeconds)}</time></header><p className="bunker-v2-mission__intro">{model.intro}</p>{model.connection==='reconnecting'&&<p role="status">Связь восстанавливается. Последнее решение и запас сохранены.</p>}<section aria-label="Запас вагона"><h2>ЧТО ЕСТЬ В ВАГОНЕ</h2><div className="bunker-v2-inventory">{model.inventory.map(item=><article key={item.itemKey}><strong>{bunkerItemLabel(item.itemKey).toLocaleUpperCase('ru-RU')}</strong><span>× {item.quantity}</span></article>)}</div></section>{model.status==='completed'?<div role="status"><h2>РАСПРЕДЕЛЕНИЕ ЗАФИКСИРОВАНО</h2><p>Последствия сохранены. Неиспользованные предметы остаются в вагоне и могут пригодиться дальше.</p></div>:<><section aria-label="Проблемы вагона"><h2>{captain?'ВЫБЕРИТЕ ДО ТРЁХ ПРОБЛЕМ':'ОБСУДИТЕ ТРИ ПРИОРИТЕТА С КАПИТАНОМ'}</h2><div className="bunker-v2-problems">{model.problems.map(problem=><label key={problem.key} className={selected.includes(problem.key)?'is-selected':''}><input type="checkbox" checked={selected.includes(problem.key)} disabled={!captain||(!selected.includes(problem.key)&&selected.length>=3)} onChange={()=>toggle(problem.key)}/><span><strong>{problem.title}</strong><small>{problem.risk}</small><em>Подходит: {bunkerItemLabel(problem.itemKey)}</em></span></label>)}</div></section>{model.ability?.available&&<aside className="bunker-v2-mission__ability"><strong>ВАША СПОСОБНОСТЬ МОЖЕТ ПОМОЧЬ</strong><p>{model.ability.label} — для проблемы «{model.problems.find(p=>p.key===model.ability?.problemKey)?.title}».</p><button type="button" disabled={busy} onClick={()=>{setBusy(true);Promise.resolve(onUseAbility?.(model.ability!.problemKey)).finally(()=>setBusy(false));}}>ПРИМЕНИТЬ МОЮ СПОСОБНОСТЬ</button></aside>}{model.pendingCommitments.length>0&&<p role="status">Предложено способностей: {model.pendingCommitments.filter(c=>c.status==='pending').length}. Они будут потрачены только если капитан выберет соответствующую проблему.</p>}{captain&&<button className="bunker-v2-mission__primary" type="button" disabled={selected.length===0||busy} onClick={()=>{setBusy(true);Promise.resolve(onConfirm?.(selected)).finally(()=>setBusy(false));}}>ПОДТВЕРДИТЬ РАСПРЕДЕЛЕНИЕ</button>}</>}</section>;
}
