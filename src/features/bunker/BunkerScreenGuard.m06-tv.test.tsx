import { act, render, screen } from '@testing-library/react';import { describe, expect, it, vi } from 'vitest';import { BunkerScreenGuard } from './BunkerScreenGuard';async function flush(){await act(async()=>{await Promise.resolve();await Promise.resolve();await Promise.resolve();});}const state={status:'active' as const,startedAt:'2026-08-30T18:50:00Z',durationSeconds:1800,remainingSeconds:600,soundEnabled:false,phase:'mission_b' as const,unlocked:false,teams:[],characterCounts:{active:15,saved:12,excluded:3},globalGameState:'MISSION_06' as const,currentMission:{id:'m6',state:'MISSION_06',plan:null},serverNow:'2026-08-30T18:50:00Z'};const m06={contractVersion:2 as const,status:'active' as const,serverNow:'2026-08-30T18:50:00Z',deadlineAt:'2026-08-30T18:58:00Z',title:'Общий протокол',fragmentsRevealed:1,fragmentsTotal:2,wagons:[{wagonId:'w1',label:'ВАГОН №1',consensusReady:false},{wagonId:'w2',label:'ВАГОН №2',consensusReady:true}]};describe('BunkerScreenGuard M06',()=>{it('renders public protocol progress without secrets',async()=>{render(<BunkerScreenGuard dependencies={{load:vi.fn().mockResolvedValue(state),loadMissionSix:vi.fn().mockResolvedValue(m06)}}><div>base</div></BunkerScreenGuard>);await flush();expect(screen.getByRole('region',{name:'Задание 6 · общий экран'})).toBeInTheDocument();expect(screen.queryByText('4719')).not.toBeInTheDocument();});});
